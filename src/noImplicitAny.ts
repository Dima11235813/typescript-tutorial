let compilerErrorFunction = function(passedObject) : string{
    return `Do stuff with ${passedObject}`
}

let anotherFunction = function(passedObject: any) : string{
    return `Do stuff with ${passedObject}`
}